
<ServerManagerConfiguration>
  <!-- This is the server manager configuration XML. It defines the interface to
       our new filter. As a rule of thumb, try to locate the configuration for
       a filter already in ParaView (in Servers/ServerManager/Resources/*.xml)
       that matches your filter and then model your xml on it -->
  <ProxyGroup name="sources">
   <SourceProxy
     name="CinemaQuery"
     class="ttkCinemaQuery"
     label="TTK CinemaQuery">
     <Documentation
        long_help="TTK cinemaQuery plugin."
        short_help="TTK cinemaQuery plugin.">
          TTK cinemaQuery plugin documentation.
     </Documentation>
     <!--<InputProperty-->
     <!--   name="Input"-->
     <!--   command="SetInputConnection">-->
     <!--   <ProxyGroupDomain name="groups">-->
     <!--     <Group name="sources"/>-->
     <!--     <Group name="filters"/>-->
     <!--   </ProxyGroupDomain>-->
     <!--   <DataTypeDomain name="input_type">-->
     <!--     <DataType value="vtkTable"/>-->
     <!--   </DataTypeDomain>-->
     <!--   <Documentation>-->
     <!--     Data-set to process.-->
     <!--   </Documentation>-->
     <!-- </InputProperty>-->

      <IntVectorProperty
         name="UseAllCores"
         label="Use All Cores"
         command="SetUseAllCores"
         number_of_elements="1"
         default_values="1" panel_visibility="advanced">
        <BooleanDomain name="bool"/>
         <Documentation>
          Use all available cores.
         </Documentation>
      </IntVectorProperty>

      <IntVectorProperty
         name="ThreadNumber"
         label="Thread Number"
         command="SetThreadNumber"
         number_of_elements="1"
         default_values="1" panel_visibility="advanced">
        <IntRangeDomain name="range" min="1" max="100" />
         <Documentation>
          Thread number.
         </Documentation>
      </IntVectorProperty>

      <IntVectorProperty
         name="DebugLevel"
         label="Debug Level"
         command="SetdebugLevel_"
         number_of_elements="1"
         default_values="3" panel_visibility="advanced">
        <IntRangeDomain name="range" min="0" max="100" />
         <Documentation>
           Debug level.
         </Documentation>
      </IntVectorProperty>

      <StringVectorProperty
        name="QueryString"
        label="SQL Statement"
        command="SetQueryString"
        number_of_elements="1"
        default_values="SELECT * FROM VolumeData">
        <Hints>
            <Widget type="multi_line"/>
        </Hints>
      </StringVectorProperty>

      <PropertyGroup panel_widget="Line" label="Input options">
        <Property name="QueryString"/>
      </PropertyGroup>

      <PropertyGroup panel_widget="Line" label="Output options">
      </PropertyGroup>

      <PropertyGroup panel_widget="Line" label="Testing">
        <Property name="UseAllCores" />
        <Property name="ThreadNumber" />
        <Property name="DebugLevel" />
      </PropertyGroup>

      <Hints>
        <ShowInMenu category="TTK - Misc" />
      </Hints>
   </SourceProxy>
 </ProxyGroup>
</ServerManagerConfiguration>
